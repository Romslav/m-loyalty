# Max Loyalty — Полный обзор системы

> **Версия документа:** 1.0  
> **Статус:** 🔄 В процессе (Part 1/7)  
> **Последнее обновление:** 2026-02-20

---

## Содержание

1. [Введение и концепция](#1-введение-и-концепция)
2. [Глоссарий](#2-глоссарий)
3. [Бизнес-модель и монетизация](#3-бизнес-модель-и-монетизация)
4. [Роли и RBAC](#4-роли-и-rbac)
5. [Auth-система](#5-auth-система) *(Part 2)*
6. [Multi-tenancy](#6-multi-tenancy) *(Part 2)*
7. [Loyalty Engine](#7-loyalty-engine) *(Part 3)*
8. [POS-интеграция](#8-pos-интеграция) *(Part 4)*
9. [Frontend Admin Panel](#9-frontend-admin-panel) *(Part 5)*
10. [Telegram Mini App](#10-telegram-mini-app) *(Part 6)*
11. [Billing & Subscriptions](#11-billing--subscriptions) *(Part 6)*
12. [Analytics](#12-analytics) *(Part 7)*
13. [Infrastructure](#13-infrastructure) *(Part 7)*
14. [Security & Compliance](#14-security--compliance) *(Part 7)*

---

# 1. Введение и концепция

## 1.1 Что такое Max Loyalty

**Max Loyalty** — это полнофункциональная multi-tenant SaaS-платформа для автоматизации программ лояльности ресторанного бизнеса. Система позволяет владельцам сетей ресторанов и кафе полностью управлять программой лояльности своих гостей: начислять и списывать баллы, строить уровни, запускать промо-акции, интегрироваться с POS-системами и взаимодействовать с гостями через Telegram.

### Ключевая идея

Традиционно рестораны используют или бумажные карточки (ненадёжно, не отслеживаемо), или дорогостоящие enterprise-решения (переусложнены, требуют месяцы внедрения). Max Loyalty занимает промежуточную нишу:

- **Простота запуска:** ресторан подключается и начинает работать за один день
- **Гибкость настройки:** правила лояльности, уровни, промо-акции — всё без кода
- **Автоматизация:** интеграция с iiko и R-Keeper — баллы начисляются автоматически при каждой оплате
- **Доступность для гостя:** Telegram Mini App — гость видит баланс, историю и акции в любой момент
- **Аналитика для владельца:** ROI программы лояльности, поведение гостей, сегментация

### Целевая аудитория

| Сегмент | Описание | Типичный клиент |
|---------|----------|-----------------|
| **Одиночные рестораны** | 1 заведение, 500–2000 гостей | Семейное кафе, пиццерия, суши-бар |
| **Небольшие сети** | 2–5 заведений, 2000–15000 гостей | Региональная сеть ресторанов |
| **Средние сети** | 5–20 заведений, 15000–50000 гостей | Городская сеть fast casual |
| **Крупные сети** | 20+ заведений | Enterprise, Custom-план |

### Ценностное предложение

Для **владельца ресторана (RestaurantAdmin):**
- Готовая программа лояльности без разработки
- Интеграция с уже используемой POS-системой
- Аналитика: кто ваши лучшие гости, какой ROI от программы
- Telegram-бот с уже готовым mini app для гостей

Для **кассира (Cashier):**
- Простой интерфейс: нашёл гостя → ввёл сумму → нажал «Начислить»
- Работает offline — синхронизируется при восстановлении сети
- Поиск по телефону, QR-коду или 6-значному коду

Для **гостя (Guest):**
- Всё в Telegram — не нужно скачивать отдельное приложение
- Баланс баллов, история транзакций, текущий уровень
- Уведомления о начислениях, акциях, днях рождения

---

## 1.2 Архитектурный обзор

Max Loyalty построена как **Modular Monolith** с возможностью горизонтального масштабирования через выделение отдельных процессов:

```
┌─────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТСКИЙ УРОВЕНЬ                        │
├──────────────┬──────────────┬──────────────┬────────────────────┤
│  Owner       │  Admin/      │  Cashier     │  Guest             │
│  Dashboard   │  Manager     │  Interface   │  Telegram Mini App │
│  (Next.js)   │  Panel       │  (Next.js)   │  (React TWA)       │
│              │  (Next.js)   │  standalone  │                    │
├──────────────┴──────────────┴──────────────┴────────────────────┤
│                     CLOUDFLARE CDN / WAF                         │
├─────────────────────────────────────────────────────────────────┤
│                    API GATEWAY (Fly.io)                          │
│              NestJS REST API  /api/v1/...                        │
├────────────────┬────────────────┬────────────────────────────────┤
│  Auth Module   │ Loyalty Engine │  Analytics Module              │
│  RBAC Module   │ POS Module     │  Billing Module                │
│  Guests Module │ Telegram Module│  Notifications Module          │
├────────────────┴────────────────┴────────────────────────────────┤
│              BullMQ Worker Process (Fly.io)                      │
│  transaction-queue │ notification-queue │ export-queue           │
│  analytics-queue   │ sync-queue                                  │
├─────────────────────────────────────────────────────────────────┤
│              CRON Process (Fly.io)                               │
│  expireBalls (daily) │ analyticsSnapshot │ birthdayPromos        │
├──────────────┬──────────────┬─────────────────────────────────── ┤
│  PostgreSQL  │    Redis     │  Cloudflare R2 (S3)                │
│  (Neon.tech) │  (Upstash)   │  файлы, экспорты                   │
└──────────────┴──────────────┴────────────────────────────────────┘
```

### Технологический стек — сводка

| Слой | Технология | Обоснование |
|------|-----------|-------------|
| **Backend API** | NestJS 10 + TypeScript | Модульность, DI, декораторы, отличная экосистема |
| **ORM** | Prisma 5 | Type-safe queries, migrations, отличный DX |
| **База данных** | PostgreSQL 15 | ACID, JSON, Full-Text Search, индексы |
| **Кэш/Очереди** | Redis 7 + BullMQ | Персистентные очереди, rate limiting, сессии |
| **Frontend** | Next.js 14 (App Router) | SSR/SSG, React Server Components, CDN |
| **UI Kit** | shadcn/ui + Tailwind | Доступные компоненты, нет vendor lock-in |
| **State** | Zustand + TanStack Query | Лёгкий global state + server state |
| **Telegram** | grammy + TWA | Лучший bot framework для TypeScript |
| **Deploy API** | Fly.io | Persistent VMs, low latency, бесплатный tier |
| **Deploy Frontend** | Vercel | Идеален для Next.js, edge functions |
| **DB Cloud** | Neon.tech | Serverless PostgreSQL, autoscale, branching |
| **Redis Cloud** | Upstash | Serverless Redis, per-request billing |
| **S3** | Cloudflare R2 | Unlimited bandwidth egress free |
| **Email** | Resend | React Email templates, отличный DX |
| **SMS** | SMS.RU | Лучшие цены для России |
| **Мониторинг** | Better Stack | Logs + uptime, дешевле Datadog |

---

# 2. Глоссарий

Полный глоссарий всех терминов, используемых в системе Max Loyalty.

## 2.1 Бизнес-термины

| Термин | Определение |
|--------|-------------|
| **Платформа** | Вся система Max Loyalty целиком, включая backend, frontend, Telegram и POS-модули |
| **Owner** | Владелец платформы Max Loyalty. Единственный пользователь с глобальным доступом ко всем тенантам |
| **Тенант (Tenant)** | Организация-клиент платформы. Например: ООО "Суши-Бар Сакура" — это один тенант со своей подпиской, ресторанами и гостями |
| **Ресторан (Restaurant)** | Конкретное заведение внутри тенанта. У одного тенанта может быть 1–N ресторанов в зависимости от тарифного плана |
| **Гость (Guest)** | Конечный потребитель — посетитель ресторана, участник программы лояльности |
| **Карта гостя (GuestCard)** | Цифровая карта лояльности гостя. Содержит баланс баллов, уровень, QR-код и 6-значный код |
| **Балл (Ball/Point)** | Единица лояльности. Гости зарабатывают баллы за покупки и тратят их при оплате |
| **Регулярный баланс** | Баллы, заработанные за реальные покупки. Могут быть потрачены на оплату |
| **Промо-баланс** | Баллы, начисленные за акции, день рождения и т.д. Могут иметь срок действия |
| **Уровень (Level)** | Статус гостя в программе лояльности. Определяется суммарными тратами (lifetimeSpent). Примеры: Bronze, Silver, Gold |
| **Правило лояльности (LoyaltyRule)** | Настройка, определяющая процент начисления баллов. Например: «10% от суммы чека» |
| **Промо-акция (LoyaltyPromo)** | Временная или триггерная акция с особым начислением баллов. Например: «День рождения — 500 бонусных баллов» |
| **Экспирация баллов** | Автоматическое сгорание баллов при неактивности гостя (настраивается в днях) |
| **Reconciliation** | Процесс сверки транзакций из POS-системы с транзакциями в базе данных Max Loyalty |
| **Тариф/План** | Уровень подписки тенанта: FREE, STANDARD, MEDIUM, PRO, ULTIMATE, CUSTOM |

## 2.2 Технические термины

| Термин | Определение |
|--------|-------------|
| **Multi-tenant** | Архитектурный паттерн, при котором одна инсталляция системы обслуживает многих клиентов (тенантов), изолируя их данные |
| **Pooled DB** | Стратегия multi-tenancy, при которой все тенанты хранят данные в одной БД, изолированные через поле `tenantId` |
| **RBAC** | Role-Based Access Control — разграничение доступа по ролям |
| **ABAC** | Attribute-Based Access Control — разграничение по атрибутам (например, «только свой ресторан») |
| **JWT** | JSON Web Token — стандарт аутентификации. Access token живёт 5 мин, refresh — 14 дней |
| **RS256** | Асимметричный алгоритм подписи JWT (RSA + SHA-256). Приватный ключ для подписи, публичный для верификации |
| **argon2id** | Алгоритм хэширования паролей, победитель Password Hashing Competition 2015. Устойчив к GPU/ASIC атакам |
| **AES-256-GCM** | Симметричный алгоритм шифрования с аутентификацией. Используется для шифрования PII-полей в БД |
| **HMAC-SHA256** | Алгоритм проверки целостности и аутентичности webhook-запросов от POS-систем |
| **BullMQ** | Redis-based система очередей для Node.js. Обеспечивает надёжную асинхронную обработку задач |
| **Webhook** | HTTP-колбэк — POS-система отправляет POST-запрос в Max Loyalty при каждом закрытии чека |
| **PUSH-режим** | POS сам отправляет данные в Max Loyalty через webhook |
| **PULL-режим** | Max Loyalty сам опрашивает POS API каждые 30 минут для получения новых транзакций |
| **Idempotency** | Свойство операции давать одинаковый результат при повторном выполнении. Критично для webhook-обработки |
| **Soft delete** | Удаление записи через установку флага `deletedAt` вместо физического удаления из БД |
| **Snapshot** | Снимок состояния правила лояльности на момент транзакции, сохраняемый в JSONB-поле |
| **Prisma** | ORM для TypeScript/Node.js с автогенерацией типов из схемы и type-safe query builder |
| **Fly.io** | Платформа для деплоя Docker-контейнеров с persistent VMs и глобальной сетью |
| **SSE** | Server-Sent Events — технология push-уведомлений от сервера клиенту (для real-time dashboard) |
| **TWA** | Telegram Web App — механизм встраивания веб-приложения прямо в Telegram |
| **QR-код** | Двумерный штрихкод, кодирующий уникальный идентификатор карты гостя |
| **6-digit код** | Шестизначный числовой код для поиска гостя кассиром без QR-сканера |
| **Grace period** | Период «прощения» — время после превышения лимита или просрочки платежа, когда сервис продолжает работать |

## 2.3 POS-термины

| Термин | Определение |
|--------|-------------|
| **iiko** | Популярная российская POS-система для ресторанов. Поддерживает webhook-интеграцию |
| **R-Keeper** | Другая популярная POS-система. Интеграция через DLL-плагин (FarCard interface) |
| **POSTransaction** | Запись о транзакции, пришедшей из POS-системы |
| **POSGuestLink** | Связь между ID гостя в POS и картой гостя в Max Loyalty |
| **FarCard** | Стандартный интерфейс плагинов лояльности для R-Keeper |
| **posGuestId** | Идентификатор гостя в POS-системе (телефон или внутренний ID) |
| **posCheckId** | Уникальный идентификатор чека в POS-системе (для идемпотентности) |

## 2.4 Аналитические термины

| Термин | Определение |
|--------|-------------|
| **MRR** | Monthly Recurring Revenue — ежемесячный повторяющийся доход |
| **ARR** | Annual Recurring Revenue — годовой повторяющийся доход (MRR × 12) |
| **Churn Rate** | Процент тенантов, отменивших подписку за период |
| **ARPU** | Average Revenue Per User — средний доход на одного тенанта |
| **LTV** | Lifetime Value — суммарный доход от тенанта за всё время |
| **RFM** | Recency, Frequency, Monetary — модель сегментации гостей по давности/частоте/сумме покупок |
| **Cohort** | Группа гостей, зарегистрировавшихся в одном временном периоде |
| **Retention** | Процент гостей, вернувшихся через N периодов после регистрации |
| **Redemption Rate** | Процент начисленных баллов, которые гости фактически потратили |
| **Loyalty ROI** | Соотношение дохода от гостей-участников программы к затратам на неё |
| **Daily Snapshot** | Ежедневный снимок аналитических метрик, сохраняемый CRON-задачей |

---

# 3. Бизнес-модель и монетизация

## 3.1 Модель SaaS с подпиской

Max Loyalty работает по классической B2B SaaS-модели с ежемесячной подпиской. Владелец платформы (Owner) получает доход от тенантов (ресторанов), которые платят за доступ к системе по выбранному тарифу.

### Тарифные планы

| План | Рестораны | Гостей | Цена/мес | Цена/год | Описание |
|------|-----------|--------|----------|----------|----------|
| **FREE** | 1 | 500 | 0 ₽ | 0 ₽ | Только для старта и тестирования |
| **STANDARD** | 1 | 2,000 | 4,000 ₽ | 48,000 ₽ (−20%) | Одиночный ресторан |
| **MEDIUM** | 3 | 6,000 | 12,000 ₽ | 144,000 ₽ (−20%) | Небольшая сеть |
| **PRO** | 5 | 15,000 | 28,000 ₽ | 336,000 ₽ (−20%) | Средняя сеть |
| **ULTIMATE** | ∞ | ∞ | договорная | договорная | Крупные сети |
| **CUSTOM** | гибко | гибко | — | — | Индивидуальные условия |

**Скидка при годовой оплате:** 20% — год за цену 9.6 месяцев.

### Дополнительные условия

- **Trial период:** 14 дней бесплатно для любого нового тенанта (кроме FREE-плана)
- **Превышение лимитов:** Grace period 90 дней при достижении лимита гостей. Предупреждение при 100%
- **Автоопродление:** включено по умолчанию, отключается в настройках
- **Yearly скидка:** при переходе на годовую оплату — 20% от ежемесячной цены

### Платёжные провайдеры

| Провайдер | Регион | Методы оплаты |
|-----------|--------|---------------|
| **YooKassa** | Россия | Карты, SBP, ЮMoney, наличные через терминалы |
| **Stripe** | Международный | Visa, Mastercard, SEPA, Apple Pay, Google Pay |
| **CloudPayments** | Россия/СНГ | Карты, Apple Pay, Google Pay |
| **CryptoCCloud** | Крипто | USDT, BTC, ETH |

## 3.2 Жизненный цикл тенанта

```
[Регистрация]
      │
      ▼
[Trial 14 дней] ──── истёк ────► [CANCELLED]
      │
    оплатил
      │
      ▼
[ACTIVE] ◄──────────────────────┐
      │                          │
   просрочка                   оплатил
      │                          │
      ▼                          │
[PAST_DUE] ──► попытки 0,1,3,7 дней ──► [CANCELLED_NONPAYMENT]
```

### Dunning (работа с просроченными платежами)

При неуспешном платеже система автоматически:
1. **День 0** — первая попытка. Email: «Не удалось списать оплату»
2. **День 1** — вторая попытка. Email: «Повторная попытка не удалась»
3. **День 3** — третья попытка. Email: «Последний шанс — обновите данные карты»
4. **День 7** — четвёртая и финальная попытка
5. После провала всех попыток — статус `CANCELLED_NONPAYMENT`, данные тенанта сохраняются 90 дней

### Превышение лимитов (Overage)

При достижении 90% лимита по гостям или ресторанам:
- Система отправляет email и уведомление в интерфейсе
- CTA: «Обновить тариф»

При достижении 100% лимита:
- Grace period 90 дней — новые гости не регистрируются, существующие работают нормально
- По истечении grace period — возможность только чтения

## 3.3 Финансовые операции

### Возвраты (Refund)

- `Payment.status = REFUNDED` — полный возврат
- `Payment.status = PARTIALLY_REFUNDED` — частичный возврат
- При возврате баллы, начисленные по транзакции, **не отменяются автоматически** — это решает Admin через `manualSubtract`
- Email уведомление тенанту о возврате

### Чарджбэки (Chargeback)

- `Payment.status = CHARGEBACK`
- Подписка переводится в `CANCELLED_CHARGEBACK`
- Owner получает уведомление о chargeback
- Тенант блокируется до выяснения обстоятельств

### Ручные инвойсы

Для тенантов на CUSTOM-плане Owner может создать manual invoice:
- Invoice создаётся в статусе `PENDING_MANUAL_PAYMENT`
- Owner вручную переводит в `PAID` после получения оплаты
- Subscription активируется/продлевается

---

# 4. Роли и RBAC

## 4.1 Иерархия ролей

Max Loyalty использует гибридную модель RBAC+ABAC:
- **RBAC** — у каждого пользователя есть роль с базовым набором permissions
- **ABAC** — дополнительные атрибутные ограничения (например, «только для своего ресторана»)

```
[OWNER]          ← Платформенная роль. Один на всю систему
    │
    └── [RESTAURANT_ADMIN] ← Тенантная роль. Один или несколько на тенант
              │
              ├── [MANAGER]  ← Ресторанная роль. Привязан к конкретному ресторану
              │       │
              │       └── [CASHIER] ← Ресторанная роль
              │
              └── [CASHIER]  ← Может быть напрямую под Admin

[GUEST]         ← Отдельная роль. Привязан через GuestCard к тенанту/ресторану
```

## 4.2 Роль: OWNER

Owner — это единственный аккаунт с глобальным доступом ко всей платформе. Это не клиент, а сам владелец продукта Max Loyalty.

### Что видит Owner

- **Platform Dashboard:** сводная статистика по всем тенантам — MRR, ARR, Churn Rate, Total Guests, активные подписки
- **Tenants Table:** список всех тенантов с фильтрами по статусу, плану, дате регистрации
- **Tenant Details:** drill-down в любой тенант — гости, транзакции, настройки loyalty
- **Billing:** история платежей всех тенантов, ручные invoices
- **Global Analytics:** агрегированные метрики по всей платформе

### Что может Owner

```typescript
// Permissions Owner
const OWNER_PERMISSIONS = [
  // Управление тенантами
  'tenant:read',
  'tenant:create',
  'tenant:update',
  'tenant:block',
  'tenant:delete',
  
  // Impersonation — вход от имени любого пользователя
  'impersonate:any',
  
  // Управление подписками
  'subscription:read',
  'subscription:create',
  'subscription:update',
  'subscription:cancel',
  'invoice:create',
  'invoice:mark_paid',
  
  // Просмотр всего
  'analytics:read:global',
  'guest:read:any',
  'transaction:read:any',
  
  // Безопасность
  'security:manage',
  'secrets:rotate',
];
```

### Owner Impersonation

Owner может войти в систему от имени любого пользователя (Admin, Manager, Cashier, Guest). Это критически важный инструмент поддержки:

1. Owner нажимает «Войти как» у выбранного тенанта
2. Система требует подтверждения MFA (TOTP или SMS)
3. Вводится причина импersonation (минимум 10 символов)
4. Создаётся `ImpersonationSession` с TTL 2 часа
5. Все действия Owner в чужом аккаунте логируются в ActivityLog с пометкой `IMPERSONATION`
6. Целевой пользователь получает уведомление: «Владелец платформы зашёл в ваш аккаунт»
7. После работы Owner нажимает «Выйти из режима импersonation» или ждёт истечения TTL

```typescript
// Пример записи в ActivityLog при импersonation
{
  action: 'IMPERSONATION_START',
  actorId: 'owner-user-id',
  targetId: 'admin-user-id',
  severity: 'CRITICAL',
  metadata: {
    reason: 'Техническая поддержка — гость не может начислить баллы',
    sessionId: 'imp-session-uuid',
    expiresAt: '2026-02-20T13:00:00Z',
    ipAddress: '192.168.1.1'
  }
}
```

## 4.3 Роль: RESTAURANT_ADMIN

RestaurantAdmin — это клиент платформы. Владелец или управляющий ресторанной сети. Имеет полный доступ ко своему тенанту.

### Ключевые возможности Admin

| Раздел | Что может |
|--------|----------|
| **Гости** | CRUD (создание, просмотр, редактирование, мягкое удаление) |
| **Транзакции** | Просмотр всех, ручная корректировка (add/subtract) с причиной |
| **Loyalty Builder** | Полное управление правилами, уровнями, промо-акциями |
| **POS Integration** | Настройка подключения к iiko/R-Keeper, просмотр статуса |
| **Аналитика** | Полная — Revenue, Guests, RFM, Cohort, ROI |
| **Команда** | Создание Manager, Cashier, их настройка и удаление |
| **Настройки** | Всё, кроме billing (billing — Owner) |
| **Экспорт** | XLSX, CSV, PDF |

### Чего НЕ может Admin

- Управлять своей подпиской (только Owner)
- Видеть других тенантов
- Создавать другого Admin (только Owner может)

### Структура данных Admin

```typescript
// В БД:
UserTenantRole {
  userId: 'uuid',
  tenantId: 'tenant-uuid',
  role: UserRole.RESTAURANT_ADMIN,
  createdAt: Date
}

// При логине JWT содержит:
{
  sub: 'user-uuid',
  email: 'admin@sushi-bar.ru',
  role: 'RESTAURANT_ADMIN',
  tenantId: 'tenant-uuid',
  permissions: ['guest:read', 'guest:create', 'loyalty:manage', ...]
}
```

## 4.4 Роль: MANAGER

Manager — менеджер одного конкретного заведения. Назначается Admin. Имеет ограниченный доступ по сравнению с Admin.

### Отличия Manager от Admin

| Функция | Admin | Manager |
|---------|-------|---------|
| Гости (CRUD) | ✅ Полный | ✅ Полный (в своём ресторане) |
| Ручная корректировка баллов | ✅ | ✅ |
| Удаление гостей | ✅ | ❌ |
| Loyalty Builder — просмотр | ✅ | ✅ |
| Loyalty Builder — изменение | ✅ | ⚠️ PENDING_APPROVAL |
| POS Integration — настройка | ✅ | ❌ |
| POS Integration — просмотр | ✅ | ✅ (read-only) |
| Аналитика | ✅ Полная | ✅ Ограниченная |
| Billing/Subscriptions | ❌ | ❌ |
| Управление командой | ✅ (своего ресторана) | ❌ |
| Создание Cashier | ✅ | ✅ |
| Export | ✅ | ✅ |

### Approval Workflow для Manager

Когда Manager пытается изменить правило лояльности, промо-акцию или уровень:

1. Изменение сохраняется как `PENDING_APPROVAL` (не применяется)
2. Admin получает уведомление: «Manager [Имя] предлагает изменение правила лояльности»
3. Admin видит diff: было → станет
4. Admin **одобряет** → изменение применяется
5. Admin **отклоняет** → изменение отбрасывается, Manager получает уведомление с причиной

```typescript
// Модель для очереди одобрений
PendingApproval {
  id: uuid
  tenantId: string
  restaurantId: string
  requestedByUserId: string   // Manager
  reviewedByUserId?: string   // Admin
  entityType: 'LOYALTY_RULE' | 'LOYALTY_LEVEL' | 'LOYALTY_PROMO'
  entityId: string
  changeType: 'UPDATE' | 'CREATE' | 'DELETE'
  changesBefore: JsonB   // Снимок до изменения
  changesAfter: JsonB    // Предлагаемые изменения
  status: 'PENDING' | 'APPROVED' | 'REJECTED'
  rejectionReason?: string
  createdAt: Date
  reviewedAt?: Date
}
```

## 4.5 Роль: CASHIER

Cashier — кассир в конкретном заведении. Минимальный набор прав, только для работы на кассе.

### Что может Cashier

- **Поиск гостя:** по телефону, QR-коду, 6-значному коду
- **Начисление баллов (Earn):** ввести сумму чека → система рассчитывает баллы
- **Списание баллов (Redeem):** гость хочет потратить баллы → ввести сумму к списанию
- **Регистрация нового гостя:** имя + телефон → создаётся GuestCard
- **Просмотр баланса гостя:** только при активной сессии (не может просматривать чужих гостей без причины)

### Что НЕ может Cashier

- Ручная корректировка баллов (только earn/redeem через POS-flow)
- Просмотр аналитики
- Управление настройками
- Изменение данных гостя (только через форму регистрации при создании)

### Аутентификация Cashier

Cashier использует особый способ входа — **PIN-аутентификацию:**
- 4-значный PIN-код
- Числовая клавиатура (numpad) с крупными кнопками 80×80px
- Автовыход через 15 минут бездействия
- Fallback: Email + Password если PIN забыт

```typescript
// PIN хранится как bcrypt hash (saltRounds: 12)
// Ограничение: 3 попытки, после — блокировка на 5 минут

const CASHIER_PIN_POLICY = {
  length: 4,
  maxAttempts: 3,
  lockoutMinutes: 5,
  sessionTimeoutMinutes: 15,
  autoLogoutOnIdleMinutes: 15
};
```

## 4.6 Роль: GUEST

Guest — гость ресторана, участник программы лояльности. Уникален в контексте тенанта (один человек может быть гостем разных ресторанных сетей).

### Способы регистрации гостя (3 пути)

**Путь 1: Через кассира**
```
Cashier открывает интерфейс кассира
→ Нажимает «Новый гость»
→ Вводит имя и телефон
→ Система создаёт User + GuestProfile + GuestCard
→ Кассир показывает QR-код / 6-digit код гостю
→ Гость сохраняет в Telegram (опционально)
```

**Путь 2: Через ссылку регистрации**
```
Admin создаёт RegistrationLink (напр. https://t.me/MaxLoyaltyBot?start=LINK_CODE)
→ Гость переходит по ссылке в Telegram
→ Бот предлагает зарегистрироваться
→ Гость вводит телефон → OTP → имя
→ Создаётся GuestCard, привязанная к ресторану из ссылки
```

**Путь 3: Автоматически из POS**
```
POS отправляет webhook с nomером телефона гостя
→ Max Loyalty не находит гостя по телефону
→ Если настройка autoCreateGuestOnPosWebhook = true:
   → Создаётся User (phone only) + GuestProfile + GuestCard
   → registrationSource = 'POS_AUTO'
→ Гость позднее может привязать Telegram через бот
```

### Данные гостя

```typescript
// Структура данных гостя
Guest = {
  // User (аутентификация)
  phone: string,          // Уникальный идентификатор
  telegramId?: string,    // Привязывается при входе через Telegram
  passwordHash?: null,    // Гости не используют пароль
  
  // GuestProfile (личные данные)
  firstName: string,
  lastName?: string,
  gender?: 'MALE' | 'FEMALE' | 'OTHER',
  birthdate?: Date,
  
  // GuestCard (карта лояльности)
  qrCode: string,          // Уникальный UUID-based QR
  code6Digit: string,      // Уникальный 6-значный код
  regularBalance: number,  // Обычные баллы
  promoBalance: number,    // Промо-баллы
  lifetimeSpent: Decimal,  // Общая сумма трат (для уровней)
  loyaltyLevel: Level,     // Текущий уровень
  
  // GuestChild (дети, для детских промо)
  children: GuestChild[]   // Имя + дата рождения
}
```

### Что видит гость в Telegram Mini App

- Текущий баланс (regular + promo раздельно)
- QR-код для предъявления кассиру
- 6-значный код
- Текущий уровень и прогресс до следующего
- История последних транзакций (50 записей)
- Активные промо-акции
- Форма редактирования профиля
- Список детей (для детских промо)
- Настройки уведомлений (opt-in/opt-out)

## 4.7 Матрица прав (Permission Matrix)

Все 30 прав системы с разбивкой по ролям:

```typescript
// Полная матрица прав
export const PERMISSION_MATRIX = {
  // --- ГОСТИ ---
  'guest:create':         { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: true,  GUEST: false },
  'guest:read:any':       { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'guest:read:own':       { OWNER: false, ADMIN: false, MANAGER: false, CASHIER: false, GUEST: true  },
  'guest:update':         { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: true  }, // Guest — только свои данные
  'guest:delete':         { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'guest:export':         { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  
  // --- БАЛЛЫ ---
  'balls:earn':           { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: true,  GUEST: false },
  'balls:redeem':         { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: true,  GUEST: false },
  'balls:manual_add':     { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'balls:manual_sub':     { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'balls:transfer':       { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: true  },
  'transaction:read':     { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: true  }, // Guest — только свои
  'transaction:cancel':   { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  
  // --- LOYALTY BUILDER ---
  'loyalty:rules:read':   { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'loyalty:rules:write':  { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false }, // Manager — через PENDING_APPROVAL
  'loyalty:levels:read':  { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'loyalty:levels:write': { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'loyalty:promos:read':  { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'loyalty:promos:write': { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  
  // --- АНАЛИТИКА ---
  'analytics:read:global':  { OWNER: true,  ADMIN: false, MANAGER: false, CASHIER: false, GUEST: false },
  'analytics:read:tenant':  { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'analytics:read:restaurant': { OWNER: true, ADMIN: true, MANAGER: true, CASHIER: false, GUEST: false },
  
  // --- POS ИНТЕГРАЦИЯ ---
  'pos:configure':        { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'pos:read_status':      { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  
  // --- КОМАНДА ---
  'team:create_manager':  { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'team:create_cashier':  { OWNER: true,  ADMIN: true,  MANAGER: true,  CASHIER: false, GUEST: false },
  'team:update':          { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  'team:delete':          { OWNER: true,  ADMIN: true,  MANAGER: false, CASHIER: false, GUEST: false },
  
  // --- БИЛЛИНГ ---
  'billing:read':         { OWNER: true,  ADMIN: false, MANAGER: false, CASHIER: false, GUEST: false },
  'billing:manage':       { OWNER: true,  ADMIN: false, MANAGER: false, CASHIER: false, GUEST: false },
  
  // --- БЕЗОПАСНОСТЬ ---
  'security:manage':      { OWNER: true,  ADMIN: false, MANAGER: false, CASHIER: false, GUEST: false },
  'impersonate:any':      { OWNER: true,  ADMIN: false, MANAGER: false, CASHIER: false, GUEST: false },
};
```

## 4.8 Реализация RBAC в NestJS

### Guards

```typescript
// Декораторы для защиты endpoints

// 1. Проверка аутентификации
@UseGuards(JwtAuthGuard)

// 2. Проверка роли
@UseGuards(JwtAuthGuard, RolesGuard)
@Roles(UserRole.RESTAURANT_ADMIN, UserRole.MANAGER)

// 3. Проверка конкретного permission
@UseGuards(JwtAuthGuard, PermissionsGuard)
@RequirePermissions('guest:create')

// 4. Step-Up Authentication (требует недавней MFA)
@UseGuards(JwtAuthGuard, StepUpAuthGuard)
@Post('admin/impersonate')

// Пример полного защищённого endpoint
@Controller('api/v1/guests')
export class GuestsController {
  
  @Get()
  @UseGuards(JwtAuthGuard, PermissionsGuard)
  @RequirePermissions('guest:read:any')
  async listGuests(
    @CurrentTenant() tenantId: string,
    @Query() filters: GuestFiltersDto
  ) {
    // tenantId автоматически извлекается из JWT
    // Все запросы к БД автоматически фильтруются по tenantId
    return this.guestsService.findAll(tenantId, filters);
  }
  
  @Delete(':id')
  @UseGuards(JwtAuthGuard, PermissionsGuard)
  @RequirePermissions('guest:delete')
  async deleteGuest(
    @CurrentTenant() tenantId: string,
    @Param('id') guestId: string
  ) {
    // Проверка: гость принадлежит этому тенанту
    return this.guestsService.softDelete(tenantId, guestId);
  }
}
```

### Permission Caching в Redis

Проверка прав — горячий путь. Каждый API-запрос проверяет права пользователя. Чтобы не ходить в БД при каждом запросе:

```typescript
@Injectable()
export class PermissionsGuard implements CanActivate {
  
  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest();
    const user = request.user; // Уже декодирован JwtAuthGuard
    
    // 1. Пробуем кэш
    const cacheKey = `permissions:${user.id}`;
    let permissions = await this.redis.get(cacheKey);
    
    if (!permissions) {
      // 2. Читаем из БД
      permissions = await this.prisma.userPermission.findMany({
        where: { userId: user.id }
      });
      
      // 3. Кэшируем на 5 минут
      await this.redis.set(
        cacheKey,
        JSON.stringify(permissions),
        'EX',
        300 // 5 минут TTL
      );
    }
    
    // 4. Инвалидируем кэш при изменении прав
    // (вызывается в RoleService.updatePermissions)
    return this.checkPermission(permissions, requiredPermission);
  }
}

// Инвалидация при изменении роли/прав
async updateUserPermissions(userId: string, permissions: string[]) {
  await this.prisma.userPermission.updateMany({ where: { userId }, data: { ... } });
  
  // Сбросить кэш немедленно
  await this.redis.del(`permissions:${userId}`);
}
```

## 4.9 Динамические permissions

Admin может тонко настроить права Manager и Cashier — добавить или убрать отдельные permissions сверх базовых для их роли:

```typescript
// Например: дать конкретному Cashier право видеть аналитику
UserPermission {
  userId: 'cashier-uuid',
  permission: 'analytics:read:restaurant',
  grantedBy: 'admin-uuid',
  grantedAt: Date,
  type: 'GRANT'  // или 'REVOKE' — для явного отзыва
}

// В UI Admin видит:
// [v] Manage guests (base Manager right)
// [v] View analytics (base Manager right)
// [ ] Manage loyalty rules  ← Manager не может по умолчанию
// [v] Create cashiers (custom granted right)
// [ ] Delete guests  ← явно отозвано
```

---

> **📌 Конец Part 1/7.** Следующая часть: Auth-система, Multi-tenancy, Rate Limiting.
> Продолжение в сессии S-02.
